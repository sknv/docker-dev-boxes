#!/usr/bin/env bash
# Create the volume to persist data across containers.

# TODO: Uncomment required volume.

# Volume for MongoDB.
# docker volume create --name=<image-name>-mongo-data

# Volume for PostgreSQL.
# docker volume create --name=<image-name>-pg-data
