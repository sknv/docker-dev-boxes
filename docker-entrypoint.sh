#!/usr/bin/env bash
# Entry point for a Docker image.

# Uncomment required entry point or use a default one...

# mongod # Start Mongod.
# postgres -D /var/lib/postgresql/data # Start PostgreSQL.
/bin/bash # Default cmd.
